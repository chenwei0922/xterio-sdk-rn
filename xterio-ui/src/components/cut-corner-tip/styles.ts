import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 3,
  },
  corner: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
  },
  text: {
    fontSize: 12,
  },
});

export default styles;
