// import { remapProps, cssInterop } from 'nativewind';
// import { XCutCornerTip, XLoading } from './components';

// // remapProps(XLoading, {
// //   className: 'style',
// // });

// // remapProps(XCutCornerTip, {
// //   className: 'style',
// // });

// // import { cssInterop } from 'nativewind';
// // cssInterop(XCutCornerTip, { className: 'style' });

// remapProps(XCutCornerTip, { className: 'style', textClassName: 'textStyle' });
