import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {},
  heading: {
    flexDirection: 'row',
    alignItems: 'center',
    height: 48,
  },
  content: {},
  text: {
    color: '#fefefe',
    flex: 1,
  },
});

export default styles;
